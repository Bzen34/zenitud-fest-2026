<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zenitud Fest 2026</title>

  <!-- PWA -->
  <link rel="manifest" href="manifest.json">
  <meta name="theme-color" content="#2D1B69">

  <!-- Icônes -->
  <link rel="icon" sizes="192x192" href="icon-192.png">
  <link rel="apple-touch-icon" href="icon-192.png">

  <style>
    body {
      font-family: sans-serif;
      background-color: #2D1B69;
      color: white;
      text-align: center;
      padding: 2rem;
    }
  </style>
</head>
<body>
  <h1>Bienvenue au Zenitud Fest 2026</h1>
  <p>Prépare-toi pour une expérience zen et festive 🌿🎶</p>

  <script>
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('./sw.js')
        .then(() => console.log('SW enregistré'))
        .catch((err) => console.log('Échec enregistrement SW', err));
    }
  </script>
</body>
</html>

{
  "document": {
    "title": "Zenitud Fest 2026",
    "size": "A5",
    "pages": [
      {
        "name": "Page 1 – Couverture",
        "elements": [
          { "type": "text", "content": "ZENITUD FEST 2026", "style": "title" },
          { "type": "text", "content": "Festival du Bien-être et de la Sérénité", "style": "subtitle" },
          { "type": "text", "content": "7 & 8 juin 2026 – Les Peupliers, Bessan", "style": "body" },
          { "type": "image", "src": "nature_peupliers.jpg", "style": "background" },
          { "type": "logo", "src": "bzen_logo.png" }
        ]
      },
      {
        "name": "Page 2 – Édito",
        "elements": [
          { "type": "text", "content": "Bienvenue au Zenitud Fest 2026 🌿", "style": "title" },
          { "type": "text", "content": "Nous sommes heureux de vous accueillir dans ce lieu magique...", "style": "body" },
          { "type": "text", "content": "Le thème 2026, « Au Fil de l’Être », est une invitation à suivre le fil d’or de votre chemin intérieur.", "style": "quote" }
        ]
      },
      {
        "name": "Page 3 – Sommaire",
        "elements": [
          { "type": "list", "items": [
            "Programme général",
            "Plans",
            "Liste des exposants",
            "Conférences & ateliers",
            "Infos pratiques",
            "Partenaires"
          ]}
        ]
      }
      // ... (pages 4 à 12 à compléter selon la structure)
    ],
    "styles": {
      "colors": {
        "primary": "#9EB69D",
        "secondary": "#F5F1E9",
        "accent": "#B5CFE6",
        "highlight": "#E2C275",
        "neutral": "#7D6354"
      },
      "fonts": {
        "title": "Playlist Script",
        "subtitle": "Raleway",
        "body": "Open Sans"
      }
    }
  }
}

{
  "name": "Zenitud Fest 2026",
  "short_name": "Zenitud",
  "start_url": "/zenitud-fest-2026/",
  "display": "standalone",
  "background_color": "#2D1B69",
  "theme_color": "#2D1B69",
  "icons": [
    {
      "src": "icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}

self.addEventListener('install', (event) => {
  console.log('Service Worker installé');
  event.waitUntil(
    caches.open('zenitud-cache-v1').then((cache) => {
      return cache.addAll([
        './',
        './index.html',
        './manifest.json',
        './icon-192.png',
        './icon-512.png'
      ]);
    })
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.map((k) => (k !== 'zenitud-cache-v1' ? caches.delete(k) : null)))
    )
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((resp) => resp || fetch(event.request))
  );
});
